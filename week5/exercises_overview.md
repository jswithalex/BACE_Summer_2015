ex 1

+ find and save the url for an image (not a webpage) of a beachball.
+ load google.com and use the developer tools to find the id of the google logo on the main page.
+ in the dev tools console, switch the beachball in for the image. 


ex 2
a. thinking about the problem: Take 5 minutes to generate a list of steps to solve this problem: given an array of strings (grocery items), add them to an unodered list ( a ul element) that you can assume already exists on the html page.
b. write that code.

ex 3
a. Thinking about the problem: Take 5 minutes to generate a list of steps to solve this problem: Given the same ul and a form input, generate a list of steps to add items to the ul using the input instead of using hardcoded strings.
b. code it.

ex 4
a. Take 5 minutes to think about the steps needed to improve example 3 by letting the user delete an item by clicking on it.
b. write the code.
